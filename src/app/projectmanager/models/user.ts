import { Note } from './note';

export class User {
    _id: string;
    username: string;
    passwordHash: string;
    // id: number;
    // birthDate: Date;
    // name: string;
    // avatar: string;
    // bio: string;

    // notes: Note[] = [];
}
